from django.urls import path
from . import views
# from .views import LoginPage, LogoutView
app_name = 'alumnos_app'

urlpatterns = [
    # path('alumnos/', views.HomePage.as_view(), name='alumnos'),

]